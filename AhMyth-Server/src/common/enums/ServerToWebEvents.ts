export enum ServerToWebEvents {
    VICTIM_LIST = 'victim-list',
    VICTIM_CONNECTED = 'victim-connected',
    VICTIM_DISCONNECTED = 'victim-disconnected',
    VICTIM_LISTENING_STATUS = 'victim-listening-status',
    PAYLOAD_LIST = 'payload-list',
    PAYLOAD_ADDED = 'payload-added',
    PAYLOAD_UPDATED = 'payload-updated',
    PAYLOAD_DELETED = 'payload-deleted',
    PAYLOAD_LOG_ADDED = 'payload-log-added',
    PAYLOAD_LOG_UPDATED = 'payload-log-updated',
}
